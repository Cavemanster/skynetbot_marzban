# VPN Бот для Marzban

Telegram бот для продажи и управления VPN подписками через панель [Marzban](https://github.com/Gozargah/Marzban).

## Возможности

### Пользовательские функции
- 🔑 **Управление VPN** - Получение ссылок для подключения и QR-кодов
- 💰 **Тарифные планы** - Несколько планов подписки с разными лимитами трафика
- 📊 **Проверка статуса** - Просмотр использования трафика и даты истечения
- 🎁 **Реферальная система** - Зарабатывайте бонусные дни, приглашая друзей
- 🆓 **Пробный период** - 3-дневный бесплатный пробный период для новых пользователей
- 💳 **Ручные платежи** - Простая оплата картой с подтверждением администратора

### Функции администратора
- 📊 **Статистика** - Статистика бота и панели Marzban
- 💰 **Управление платежами** - Подтверждение/отклонение платежей
- 👥 **Управление пользователями** - Поиск, просмотр и управление пользователями
- 🚫 **Бан/Разбан** - Контроль доступа пользователей
- 📢 **Рассылка** - Отправка сообщений всем пользователям
- 🔍 **Поиск пользователей** - Поиск по ID или username

## Структура проекта

```
skynetbot_marzban/
├── main.py                 # Точка входа бота
├── marzban_client.py       # Клиент для API Marzban
├── database.py             # Операции с SQLite базой данных
├── handlers_user.py        # Обработчики для пользователей
├── handlers_admin.py       # Обработчики для администраторов
├── background_tasks.py     # Периодические задачи (уведомления, очистка)
├── keyboards.py            # Inline и reply клавиатуры
├── states.py               # Состояния FSM
├── data/
│   └── tarifs.json         # Конфигурация тарифных планов
├── logs/                   # Логи бота (создаётся автоматически)
├── .env.example            # Шаблон конфигурации
├── requirements.txt        # Python зависимости
├── Dockerfile              # Docker конфигурация
├── docker-compose.yml      # Docker Compose настройка
└── deploy_bot.sh           # Скрипт развёртывания
```

## Требования

- Python 3.11+
- Панель Marzban (v0.5.0+)
- Токен Telegram бота
- Docker & Docker Compose (опционально)

## Публикация на GitHub

### 1. Инициализация репозитория

```bash
cd /Users/aleksejarhipockin/Documents/skynetbot_marzban
git init
```

### 2. Создание .gitignore (уже есть в проекте)

Файл `.gitignore` уже создан и исключает:
- `.env` (конфигурация с секретами)
- `data/*.db` (база данных)
- `logs/` (логи)
- `__pycache__/` (кэш Python)

### 3. Добавление файлов

```bash
git add .
```

### 4. Первый коммит

```bash
git commit -m "Initial commit: VPN bot for Marzban"
```

### 5. Создание репозитория на GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите **New** (или + → New repository)
3. Введите имя: `skynetbot_marzban`
4. Выберите видимость (Private/Public)
5. **Не добавляйте** README/.gitignore (у вас уже есть)
6. Нажмите **Create repository**

### 6. Привязка удалённого репозитория

```bash
# По SSH (рекомендуется):
git remote add origin git@github.com:aleksejarhipockin/skynetbot_marzban.git

# Или по HTTPS:
git remote add origin https://github.com/aleksejarhipockin/skynetbot_marzban.git
```

### 7. Отправка на GitHub

```bash
# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код
git push -u origin main
```

### 8. Последующие изменения

```bash
# После внесения изменений:
git add .
git commit -m "Описание изменений"
git push
```

### Настройка SSH-ключа для GitHub

```bash
# Генерация SSH-ключа (если нет)
ssh-keygen -t ed25519 -C "your_email@example.com"

# Просмотр публичного ключа
cat ~/.ssh/id_ed25519.pub

# Добавьте ключ в GitHub:
# Settings → SSH and GPG keys → New SSH key
```

### Проверка подключения

```bash
# Проверка SSH-подключения
ssh -T git@github.com

# Должно появиться: Hi username! You've successfully authenticated...
```

### Проверка статуса

```bash
git status      # Показать изменения
git log         # История коммитов
git remote -v   # Показать удалённые репозитории
```

## Установка

### 1. Клонирование репозитория

```bash
git clone <url-репозитория>
cd skynetbot_marzban
```

### 2. Настройка окружения

```bash
cp .env.example .env
nano .env  # Отредактируйте с вашей конфигурацией
```

### Параметры конфигурации

| Переменная | Описание | Обязательно |
|------------|----------|-------------|
| `BOT_TOKEN` | Токен Telegram бота от @BotFather | ✅ |
| `ADMIN_USER_IDS` | Разделённые запятой Telegram ID администраторов | ✅ |
| `MARZBAN_PANEL_URL` | URL вашей панели Marzban | ✅ |
| `MARZBAN_USERNAME` | Имя пользователя администратора Marzban | ✅ |
| `MARZBAN_PASSWORD` | Пароль администратора Marzban | ✅ |
| `MARZBAN_SUBSCRIPTION_URL_PREFIX` | Префикс URL подписки | ❌ |
| `PAYMENT_CARD_NUMBER` | Номер карты для платежей | ❌ |
| `PAYMENT_CARD_HOLDER` | Имя владельца карты | ❌ |
| `TG_CHANNEL` | Ссылка на Telegram канал | ❌ |
| `SUPPORT_URL` | Ссылка на поддержку | ❌ |
| `REF_BONUS_DAYS` | Бонусных дней за реферала | ❌ |
| `VERIFY_SSL` | Проверять SSL сертификаты | ❌ |

### 3. Настройка тарифов

Отредактируйте `data/tarifs.json` для настройки тарифных планов:

```json
{
    "tariffs": [
        {
            "id": "basic",
            "name": "🥉 Базовый",
            "price": 100,
            "traffic_gb": 100,
            "duration_days": 30,
            "max_ips": 2,
            "is_trial": false
        }
    ]
}
```

### 4. Развёртывание

#### Вариант A: Docker (Рекомендуется)

```bash
# Сделать скрипт развёртывания исполняемым
chmod +x deploy_bot.sh

# Запустить развёртывание
./deploy_bot.sh
```

#### Вариант B: Ручная установка

```bash
# Установить зависимости
pip install -r requirements.txt

# Запустить бота
python main.py
```

#### Вариант C: Docker Compose

```bash
docker-compose up -d
```

## Использование

### Команды пользователя

| Команда | Описание |
|---------|----------|
| `/start` | Запустить бота |
| `/admin` | Открыть панель администратора (только для админов) |

### Меню бота

- 🔑 **Мой VPN** - Управление VPN подпиской
- 💰 **Тарифы** - Просмотр и покупка тарифных планов
- 📊 **Статус** - Проверка статуса подписки
- 🎁 **Рефералы** - Реферальная программа
- ℹ️ **Помощь** - Помощь и поддержка
- 👤 **Профиль** - Профиль пользователя

### Команды администратора

| Команда | Описание |
|---------|----------|
| `/admin` | Открыть панель администратора |

### Панель администратора

- 📊 **Статистика** - Просмотр статистики бота и Marzban
- 💰 **Платежи** - Проверка и подтверждение платежей
- 👥 **Пользователи** - Поиск и управление пользователями
- 📢 **Рассылка** - Отправка сообщений всем пользователям

## Процесс оплаты

1. Пользователь выбирает тарифный план
2. Бот отображает реквизиты для оплаты (номер карты, комментарий)
3. Пользователь производит оплату и нажимает "Подтвердить оплату"
4. Администратор получает уведомление
5. Администратор проверяет и подтверждает/отклоняет платёж
6. Пользователь получает уведомление, подписка активируется

## Фоновые задачи

Бот автоматически выполняет следующие задачи:

| Задача | Частота | Описание |
|--------|---------|----------|
| **Проверка истёкших** | Каждый час | Удаление истёкших подписок |
| **Уведомления об истечении** | Каждый час | Уведомление пользователей до истечения (24ч, 48ч, 72ч) |
| **Синхронизация трафика** | Каждый час | Синхронизация использования трафика из Marzban |
| **Очистка платежей** | Ежедневно | Удаление старых обработанных платежей |

## Логи

Логи бота сохраняются в `logs/bot.log`

Просмотр логов Docker:
```bash
docker-compose logs -f
```

## База данных

Бот использует SQLite базу данных в `data/users.db`:

- **users** - Учётные записи пользователей
- **subscriptions** - Активные подписки
- **payments** - Записи о платежах
- **referrals** - Реферальные связи

## Решение проблем

### Бот не запускается

1. Проверьте конфигурацию в `.env`
2. Убедитесь, что токен бота действителен
3. Проверьте доступность панели Marzban
4. Просмотрите логи: `docker-compose logs`

### Не удаётся подключиться к Marzban

1. Убедитесь, что `MARZBAN_PANEL_URL` указан верно
2. Проверьте учётные данные администратора
3. Убедитесь, что панель Marzban запущена
4. Проверьте настройки SSL (`VERIFY_SSL`)

### Платежи не работают

1. Убедитесь, что `PAYMENT_CARD_NUMBER` настроен
2. Убедитесь, что ID администраторов указаны верно
3. Проверьте, что бот может отправлять сообщения пользователям

## Примечания по безопасности

- ⚠️ **Никогда не коммитьте файл `.env`** - Содержит конфиденциальные данные
- ⚠️ **Используйте HTTPS** для URL панели Marzban
- ⚠️ **Ограничьте доступ администратора** - Только доверенные Telegram ID
- ⚠️ **Регулярное резервное копирование** - Создавайте резервные копии `data/users.db`

## Лицензия

Этот проект предоставляется "как есть" в образовательных целях.

## Поддержка

По вопросам и предложениям создавайте issue в репозитории.

## Благодарности

- [Marzban](https://github.com/Gozargah/Marzban) - Панель управления VPN
- [aiogram](https://github.com/aiogram/aiogram) - Фреймворк для Telegram Bot API
- Вдохновлено [VPN_bot_for_3X-UI](https://github.com/Major-Woolfi/VPN_bot_for_3X-UI)
